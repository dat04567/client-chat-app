name: React CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 🔹 Checkout code
        uses: actions/checkout@v3

      - name: 🔹 Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: 🔹 Install dependencies
        run: npm install

      - name: 🔹 Lint code (Kiểm tra lỗi code)
        run: npm run lint

      - name: 🔹 Run tests (Chạy test tự động)
        run: npm test

      - name: 🔹 Build React app
        run: npm run build
